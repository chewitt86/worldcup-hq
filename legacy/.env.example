# Copy this to ".env" (which is git-ignored) and fill in. docker compose reads it automatically.
ADMIN_PASSWORD=please-change-me
# Optional — you can instead paste the key in the in-app Admin panel (recommended):
APIFOOTBALL_KEY=

# Advanced / alternative feeds (usually leave blank):
AUTOFETCH_URL=
WC2026_BASE=
WC2026_TOKEN=
